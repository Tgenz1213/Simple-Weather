# Example environment file for Simple-Weather
# Copy this to .env.local or run `task env-setup` to generate an editable copy.
# ENV_MODE controls which Redis backend to use:
#  - srh      => run local SRH container (default for development)
#  - upstash  => use Upstash Redis (set UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN)
ENV_MODE=srh

# --- SRH (serverless-redis-http) settings ---
SRH_MODE=env
SRH_TOKEN=your_token_here
SRH_CONNECTION_STRING=redis://localhost:6379
SRH_PORT=8080

EMAIL=your.email@example.com
VITE_EMAIL=${EMAIL}

# --- Upstash settings (only used when ENV_MODE=upstash) ---
# Example: https://<id>.upstash.io
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

